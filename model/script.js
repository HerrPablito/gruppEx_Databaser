const createDbConnection = require('./db');
const db = createDbConnection();


// db.run(`INSERT INTO users VALUES ("x5", "Mart", "pwd123") `)
 
// db.run(`INSERT INTO subscriptions VALUES ("x1", "2") `)

// db.run(`DELETE FROM users WHERE user_name = "p" `)