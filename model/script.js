const createDbConnection = require('./db');
const db = createDbConnection();


// db.run(`INSERT INTO users VALUES ("x5", "Mart", "pwd123") `)
 
//db.run(`INSERT INTO subscriptions (user_id, channel_name) VALUES ("x1", "2")`)

//db.run(`DELETE FROM subscriptions WHERE subs_id > 3 `)
